language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install unittest
  - pip install codecov
install:
  - pip install unittest
  - pip install codecov
after_success:
  - codecov # submit coverage
